# אלגוריתם בניית האתרים — גישת "בנייה מחדש מאפס" (RebuildDesigner)

הענף `main`. הרעיון המרכזי: **קודם מבינים את האתר, ואז בונים אותו מחדש מאפס בפורמט
שאנחנו שולטים בו** — במקום לערוך markup זר ושברירי. כך התוצאה אחידה, תמיד מתרנדרת,
ונקייה מטביעות AI, בלי תלות באיך האתר המקורי נבנה.

> הגישה הקודמת (עריכה/הטלאה של הקוד הקיים) נשמרה בענף `transform-approach`.

---

## מבט-על של הצינור

```
ingest → detect (110 סימנים + ציון) → REBUILD (מאפס) → features (בחירה) → after-scan (ציון אחרי) → deliver
```

כל שלב שרץ מול המודל מפוצל ל**קריאות נפרדות מונעות-לקוח** (client-driven), כי לפונקציית
edge יש קיר קשיח של ~150 שניות לכל קריאת HTTP. פיצול = כל קריאה מקבלת 150 שניות טריות.

---

## שלב 0 — Ingest
המשתמש מפנה לקוד המקור (קבצים / zip / ריפו GitHub). הקבצים נארזים ל-`bundle.txt`
בפורמט:
```
=== FILE: path ===
<תוכן>

```
ונשמרים ב-Storage הפרטי תחת `{user_id}/{scan_id}/bundle.txt`.

## שלב 1 — Detect (אבחון)
פונקציה: `detect`. מריצה את הקוד מול **110 סימני "טביעת AI"** (`signals.json`).
- **Recall אגרסיבי**: מחפש לעומק, מדווח סימן כ-present רק עם ציטוט מילולי מהקוד. לעולם לא ממציא.
- **Re-hunt**: מבט נוסף על הסימנים שסומנו absent, כדי להעלות recall בלי לפברק.
- מפוצל למספר מעברים (`parts`), כל מעבר אחראי לחלק מהסימנים.

### מנגנון הציון (דטרמיניסטי)
```
score = round( 100 × Σ(משקל של סימנים present & applicable) ÷ Σ(משקל של כל הישימים) )
משקלים: high/very-high = 3, medium = 2, low = 1
```
**0 = אנושי לגמרי · 100 = AI מובהק.** נשמר ב-`ai_fingerprint_score` + `present_count`.

---

## שלב 2 — REBUILD (הלב של הגישה)
פונקציה: `rebuild`. בונה את האתר מחדש בפורמט קנוני משלנו. מונע-לקוח, `part` אחד לכל שלב:

### part 1 — מפרט תוכן (Content Spec)
לא מוציא markup אלא **מודל תוכן מובנה** (`spec.json`):
- `meta`: שם, מטרה, שפה, קהל, טון, `dir` (rtl/ltr).
- `facts`: **כל העובדות האמיתיות** — מספרים, מחירים, שמות, תאריכים, יצירת קשר. זהו **חוזה**:
  שלבי הבנייה רשאים להשתמש **רק** בעובדות שכאן. זה מה שמונע המצאת תוכן.
- `sections[]`: הסקשנים לפי הסדר — לכל אחד סוג, כותרת, טקסט, פריטים, CTA.
- `components[]`: רכיבים אינטראקטיביים (חידון/טופס) + ה-`container_id` וה-`<script>` שמפעיל אותם.
- **מנקה כל אמוג'י** מהתוכן. גם מפיק `design_direction` (פלטה, טיפוגרפיה, עיקרון פריסה, אופי).
- ה-`<script>` המקוריים נשלפים ונשמרים byte-for-byte (המודל לא רואה ולא כותב JS).

### part 2 — Shell (שפת העיצוב הגלובלית)
מייצר: `tokens_css` (מערכת design-tokens: משתני CSS, type scale, מרווחים, מחלקות רכיבים),
`head_extras` (קישורי פונטים), `header_html`, `footer_html`.

### parts 3..(2+S) — סקשן אחד לכל קריאה
בונה `html` + `css` לסקשן בודד, תוך שימוש **רק** בתוכן של אותו סקשן ובטוקנים מה-shell.
משמר `container_id` של רכיבים אינטראקטיביים.

### הרכבה (דטרמיניסטית, בקריאה האחרונה)
מרכיב מסמך HTML שלם ועצמאי: `<head>` (meta + פונטים + `tokens_css` + כל ה-CSS של הסקשנים)
+ `<body>` (header + סקשנים + footer + ה-`<script>` שנשמרו). נכתב ל-`edited-bundle.txt`.

### חוקי ברזל
- **נאמנות לעובדות**: אסור להמציא/להשמיט עובדה. `facts` הוא החוזה.
- **אפס אמוג'י** — אמוג'י הוא סימן ה-AI המובהק ביותר (#109). אסור לחלוטין, בכל מקום.
- **לשמר פונקציונליות** — נושאים את ה-`<script>` המקורי ומחברים לפי `id`.
- **לא לייצר תמונות** — משאירים נתיבי נכסים מקוריים.

---

## שלב 3 — Features (הצע → בחר → הוסף)
פונקציה: `features`.
1. **part 1** — קוראת ומבינה את האתר ומציעה **5 פיצ'רים** שמתאימים לתוכן (`features.json`). אין קוד עדיין.
2. המשתמש **בוחר** אילו פיצ'רים (1 או יותר) ב-UI.
3. לכל פיצ'ר נבחר → קריאה נפרדת עם `feature_index` שמממשת אותו ומזריקה לאתר. אסור אמוג'י.

## שלב 4 — After-Scan (ציון "אחרי", כן ומדיד)
מריצים את `detect` שוב עם `mode:"after"` על האתר החדש (edited-bundle מונח מעל המקורי),
עם **אותה נוסחת ציון בדיוק**. נשמר ב-`ai_fingerprint_score_after` (ו-`detection_after` נפרד
כדי לא לדרוס את ה"לפני"). ה-UI מציג **לפני → אחרי** + הפרש הנקודות. נמדד, לא מוצהר.

## שלב 5 — Deliver
`package` בונה zip בדפדפן; אפשר גם Pull Request ל-GitHub (ענף נפרד, לעולם לא באתר החי).

---

## אילוצים והחלטות מפתח
- **קיר 150 שניות לכל קריאה** → כל שלב יקר מפוצל לקריאות נפרדות מונעות-לקוח. אסור לצבור בתוך קריאה אחת.
- **Apply דטרמיניסטי** (בגישה הישנה) — בגישה הזו אין apply; ההרכבה דטרמיניסטית.
- **פורמט קנוני** — כי הפלט שלנו, אנחנו מבטיחים פונטים נטענים, אפס קלישאות AI, קוהרנטיות, ורינדור.

## קבצי Storage לכל scan
```
{uid}/{sid}/bundle.txt            קלט (מקור)
{uid}/{sid}/spec.json             מודל התוכן + design_direction + scripts
{uid}/{sid}/shell.json            tokens_css + header/footer + fonts
{uid}/{sid}/rebuild-sections.json הסקשנים הבנויים (מצטבר)
{uid}/{sid}/features.json         5 הפיצ'רים המוצעים
{uid}/{sid}/edited-bundle.txt     הפלט המורכב (התוצר)
```

## פונקציות edge
`detect` · `rebuild` · `features` · `design`+`apply`+`qa` (ירושה מהגישה הישנה) · `transform`
(ישן) · `package` · `push-github` · `fetch-repo` · `list-repos`
