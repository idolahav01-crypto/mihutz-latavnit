# הפעלת סליקה עם Lemon Squeezy

**כל הקוד כבר כתוב ונדחף לגיטהאב.** החנות מחוברת לצ'ק‑אאוט אמיתי, יש פונקציית
webhook שמזכה את היתרה, והכל מכוסה בבדיקות (465 עוברות).

מה שנשאר זה החלק שאני לא יכול לעשות במקומך, כי הוא דורש את החשבונות שלך:
לפתוח חשבון ב‑Lemon Squeezy, ליצור מוצר, ולתת לי ארבעה ערכים. בערך 20 דקות.

עד שזה קורה, החנות אומרת "התשלום עדיין לא פעיל" ולא מנסה לגבות כלום.

---

## מה אני צריך שתעשה — לפי הסדר

### שלב 1 — חשבון וחנות (10 דקות, כולל אימות)

1. נרשמים ב‑[lemonsqueezy.com](https://lemonsqueezy.com).
2. **Settings → Stores** — יוצרים חנות. שם, לוגו, וכתובת האתר.
3. מגדירים **מטבע החנות = USD**. כל המחירים בעמוד החנות שלנו בדולרים, ומטבע
   אחר יגרום לכך שהמספר שהלקוח רואה אצלנו והמספר שהוא משלם לא יהיו זהים.
4. ממלאים את פרטי העסק ל‑payouts (חשבון בנק, פרטי מס). בלי זה החנות נשארת
   ב‑Test mode ולא מקבלת כסף אמיתי.

> Lemon Squeezy הם **Merchant of Record** — הם מוכרים ללקוח, גובים את המע"מ
> הנכון לפי מדינת הלקוח, ומעבירים לך תשלום מרוכז. זו בדיוק הסיבה שהם בחירה
> טובה למכירה בינלאומית מישראל: אתה לא צריך להתעסק עם מע"מ באירופה בעצמך.

### שלב 2 — מוצר אחד, שממנו נמכר הכל (5 דקות)

**חשוב:** לא יוצרים ארבעה מוצרים לארבע החבילות. יוצרים **מוצר אחד**, והשרת
קובע את המחיר בכל קנייה לפי הכמות. ככה גם "כמות משלכם" עובדת.

1. **Products → New Product**
   - Name: `תשנה — טוקנים` (או באנגלית, מה שיופיע ללקוח בקבלה)
   - Pricing model: **Standard / One‑time payment** (לא Subscription)
   - Price: `$10` — זה רק ברירת מחדל, השרת דורס אותה בכל צ'ק‑אאוט
2. **Publish** את המוצר.
3. נכנסים למוצר → לשונית **Variants** → לוחצים על הווריאנט → מעתיקים את
   ה‑**Variant ID** (מספר, למשל `987654`).

### שלב 3 — מפתח API (2 דקות)

**Settings → API → +** ליצירת מפתח חדש. מעתיקים אותו **מיד** — הוא מוצג פעם
אחת בלבד.

### שלב 4 — Webhook (3 דקות)

**Settings → Webhooks → +**

- **Callback URL:**
  `https://iizcvlkktkbftbuvxlvw.supabase.co/functions/v1/lemon-webhook`
- **Signing secret:** ממציאים מחרוזת אקראית ארוכה (30+ תווים) ושומרים אותה.
  זה מה שמוכיח לשרת שהפנייה באמת הגיעה מ‑Lemon Squeezy ולא ממישהו שמצא את
  הכתובת. **זה הדבר הכי רגיש בכל התהליך.**
- **Events:** מסמנים `order_created` ו‑`order_refunded`. לא צריך יותר.

---

## מה אני צריך שתיתן לי

ארבעה ערכים. תשלח לי אותם ואני מגדיר אותם בשרת ופורס:

| מה | איפה מוצאים | דוגמה |
|---|---|---|
| `LEMONSQUEEZY_API_KEY` | Settings → API | `eyJ0eXAiOiJKV1Qi...` |
| `LEMONSQUEEZY_STORE_ID` | Settings → Stores, המספר ליד שם החנות | `12345` |
| `LEMONSQUEEZY_VARIANT_ID` | המוצר → Variants | `987654` |
| `LEMONSQUEEZY_WEBHOOK_SECRET` | המחרוזת שהמצאת בשלב 4 | `k9Fp...` |

**איך לשלוח אותם:** לא בצ'אט הזה ולא בגיטהאב. הכי פשוט — אתה מדביק אותם
בעצמך ב‑Supabase: **Project Settings → Edge Functions → Secrets → Add new
secret**, ארבע פעמים, בשמות המדויקים מהטבלה. אז אתה רק אומר לי "הוספתי",
ואני פורס. מפתח שעובר בצ'אט הוא מפתח שצריך להחליף אחר כך.

בנוסף צריך להיות מוגדר `SITE_URL` (הכתובת של האתר, למשל
`https://tashaneh.com`) — לשם הלקוח חוזר אחרי התשלום. אם הוא כבר מוגדר, אין
מה לעשות.

---

## מה אני עושה אחרי שתגיד "הוספתי"

1. מריץ את המיגרציה `0014_purchases.sql` (טבלת הרכישות + הזיכוי החד‑פעמי).
2. פורס את שתי הפונקציות:
   ```bash
   npx --yes supabase@2.115.0 functions deploy checkout --project-ref iizcvlkktkbftbuvxlvw
   npx --yes supabase@2.115.0 functions deploy lemon-webhook --project-ref iizcvlkktkbftbuvxlvw --no-verify-jwt
   ```
   ה‑`--no-verify-jwt` על ה‑webhook הוא חובה: Lemon Squeezy פונה לשם בלי
   טוקן התחברות. מה שמגן על הכתובת הוא בדיקת החתימה בתוך הפונקציה.
3. עושים יחד קנייה אחת ב‑**Test mode** עם כרטיס הבדיקה `4242 4242 4242 4242`,
   ומוודאים שהיתרה עלתה. אחרי שזה עובד — מעבירים את החנות ל‑Live.

---

## שאלות שכדאי שתחליט לגביהן עכשיו

1. **החזרים.** המערכת **רושמת** החזר אבל לא מורידה טוקנים אוטומטית. הסיבה:
   עד שההחזר מגיע הלקוח כבר יכול היה לנצל את הטוקנים, וקיזוז עיוור או
   מתנגש עם המגבלה שיתרה לא יורדת מתחת לאפס, או משאיר לקוח באמצע בנייה בלי
   טוקנים ובלי הסבר. ההחזר מופיע ב‑`purchase_log` ואתה מחליט ידנית. אם אתה
   רוצה התנהגות אחרת — תגיד ואשנה.
2. **חשבוניות בישראל.** Lemon Squeezy מוציאים קבלה ללקוח, אבל הם לא מוציאים
   חשבונית ישראלית בשמך. אם אתה צריך חשבונית לכל עסקה — צריך לחבר את זה
   לתוכנת הנהלת חשבונות בנפרד. שווה לשאול רואה חשבון לפני שעוברים ל‑Live.
3. **מדיניות ביטולים ותנאי שימוש.** Lemon Squeezy דורשים קישור למדיניות
   החזרים בעמוד התשלום. אם אין לך עמוד כזה באתר, זה חוסם מעבר ל‑Live.

---

## מה כבר עובד, בלי שתעשה כלום

- **המחיר לא נקבע בדפדפן.** העמוד שולח כמות בלבד; השרת מתמחר אותה מהעותק
  שלו ומזהה את הקונה מה‑JWT. לקוח שיערוך את העמוד אצלו יכול להזמין כמות
  אחרת — במחיר האמיתי של אותה כמות, לחשבון שלו. זה הכל.
- **תשלום מזכה פעם אחת בדיוק.** Lemon Squeezy שולחים את אותו webhook שוב ושוב
  עד שהם מקבלים תשובה תקינה; הזיכוי נעול על מזהה ההזמנה שלהם, והפנייה השנייה
  לא מוסיפה כלום.
- **הכפתור ננעל בזמן הפנייה.** לחיצה שנייה היא צ'ק‑אאוט שני, וצ'ק‑אאוט שני
  הוא חיוב שני.
- **בחזרה מהתשלום**, העמוד מחכה ליתרה שהוא מצפה לה במקום להדפיס מספר שהוא לא
  ראה. אם ה‑webhook מתעכב, הוא אומר שהתשלום נרשם — כי הוא נרשם.
