# מסירה לסשן חדש — מחוץ לתבנית

העתק את כל הקובץ הזה כהודעה ראשונה בסשן חדש.

עודכן: 19.8.2026

---

אתה ממשיך עבודה על הפרויקט הזה במקום סשן קודם. קרא את כל המסמך לפני שאתה עושה משהו.

## דבר ראשון — איך לדבר איתי

- ענה **בעברית פשוטה וברורה**, בלי מושגים טכניים מיותרים ובלי ז'רגון. כשאתה חייב מונח טכני, הסבר אותו במילה.
- **בלי אמוג'י** בשום מקום — לא בתשובות ולא בקוד. זה גם אחד מ-110 הסימנים שהמוצר עצמו מחפש.
- אל תגיד לי להריץ סריקה או בנייה **עד שאתה בטוח במאה אחוז** שהקוד שכתבת באמת רץ. כל ריצה עולה כסף אמיתי, בערך דולר וחצי עד שניים לבנייה מלאה.
- כשאתה לא בטוח — תגיד את זה במפורש, ותפריד בין מה שמובטח (קוד דטרמיניסטי עם בדיקות) לבין מה שתלוי בהתנהגות של מודל.
- אל תסתפק בקריאת קוד. **תמדוד מול הנתונים האמיתיים** במסד ובאחסון לפני שאתה מסיק מסקנה. יש שם היסטוריה של כל ריצה שרצה אי פעם, והיא בחינם.
- כשאני מבקש להסביר משהו — תסביר, אל תתחיל לתקן. וכשאני אומר "תעשה מה שצריך" — תעשה הכל ואל תעצור לשאול.

## מה המוצר עושה

אתר בעברית ובאנגלית שמקבל קוד מקור של אתר, מאבחן אותו מול **110 "סימני AI"** (טביעות אצבע של אתרים שנבנו במחוללים אוטומטיים), נותן ציון, ואז בונה את האתר מחדש מאפס בלי הסימנים האלה.

- Frontend: HTML/CSS/JS סטטי, ללא framework. הדשבורד ב-`he/app/` ו-`app/`, כל הלוגיקה ב-`js/app.js`.
- Backend: Supabase Edge Functions ב-Deno, תחת `supabase/functions/`.
- אירוח: Vercel, פריסה אוטומטית מ-`main` בגיטהאב.
- מודלים: Sonnet 5 לאבחון, Opus 4.8 לבנייה, דרך `_shared/anthropic.ts`.

השלבים המרכזיים: `detect` (אבחון), `rebuild` (בנייה מאפס), `features` (הצעת פיצ'ר ובנייתו), `package` (הורדת zip), `push-github` (פתיחת Pull Request).

## דברים תפעוליים שחייבים לדעת

**פריסה לשרת עובדת מהטרמינל.** ה-CLI מחובר, יש אסימון שמור. אל תבקש ממני סיסמה או מפתח — צרור המפתחות במק שלי קורס, אבל זה חוסם רק התחברות מחדש, לא את האסימון הקיים.

```bash
npx --yes supabase@2.115.0 functions deploy <name> --project-ref iizcvlkktkbftbuvxlvw
```

**שים לב:** ה-CLI נעלם מהמטמון של npx באמצע הסשן האחרון, ו-`npx --no-install` נכשל. אם זה קורה — `npx --yes supabase@2.115.0` מתקין אותו מחדש ועובד. `supabase/.temp/` כבר ב-gitignore.

מזהה הפרויקט: `iizcvlkktkbftbuvxlvw`. יש גם מחבר MCP ל-Supabase לשאילתות SQL ולמיגרציות.

**חשוב:** `_shared/` נארז לתוך כל פונקציה בנפרד. אם שינית קובץ ב-`_shared/`, צריך לפרוס מחדש **את כל הפונקציות שמייבאות אותו** — לא רק אחת. ככה:

```bash
grep -rln "_shared/<file>.ts" supabase/functions/*/index.ts
```

בסשן האחרון כמעט פספסתי את זה פעמיים.

**בדיקות:**
```bash
npx --no-install deno test --allow-read --allow-net supabase/functions/_shared/
npx --no-install deno check supabase/functions/<name>/index.ts
```
כרגע **231 בדיקות עוברות**. אל תפרוס בלי להריץ אותן.

**בדיקה בדפדפן:** יש כלי preview מובנה. תמיד לוודא שגודל החלון אמיתי לפני שמודדים — בסשן האחרון בזבזתי זמן על מדידות בחלון ברוחב אפס והסקתי מהן מסקנות שגויות.

**יש פרצת פיתוח מכוונת** בשם `DEV_NO_AUTH` ב-`js/app.js`, שעוקפת את מסך ההתחברות. היא עובדת רק על localhost. השארתי אותה בכוונה — אל תמחק.

**אין לך גישה להריץ סריקה או בנייה בעצמך** — הן דורשות התחברות שלי דרך הדשבורד. אתה יכול לקרוא כל תוצאה במסד, ולהוריד את קבצי הבאנדל מהאחסון:
```bash
npx --yes supabase@2.115.0 storage ls "ss:///scans/<user_id>/<scan_id>/" --experimental
npx --yes supabase@2.115.0 storage cp "ss:///scans/<...>/bundle.txt" ./local.txt --experimental
```
זה מאפשר לך להוכיח דברים בלי לבזבז כסף. **תשתמש בזה הרבה.** רוב הממצאים החשובים בסשן האחרון הגיעו משם.

לכל סריקה יש באחסון: `bundle.txt` (המקור), `edited-bundle.txt` (התוצאה), `spec.json`, `shell.json`, `rebuild-sections.json`.

## איך למדוד תוצאות

הכל בטבלה `scans`. עמודות חשובות: `detection` (לפני), `detection_after` (אחרי), `ai_fingerprint_score`, `present_count`, אותם שניים עם סיומת `_after`, `stage_usage` (עלות וזמן לכל שלב), `total_cost_usd`, `site_url`, ו-`self_check` (חדש).

השאילתה הכי שימושית — מה הבנייה ניקתה מול מה שהיא יצרה:

```sql
with b as (select (x->>'id')::int sig, x->>'name' nm, (x->>'present')::bool p
           from scans, jsonb_array_elements(detection->'signals') x where left(id::text,8)='XXXXXXXX'),
     a as (select (x->>'id')::int sig, (x->>'present')::bool p, (x->>'confidence')::float c
           from scans, jsonb_array_elements(detection_after->'signals') x where left(id::text,8)='XXXXXXXX')
select case when b.p and not a.p then 'CLEARED' else 'INTRODUCED' end kind, b.sig, b.nm, a.c
from b join a using (sig) where b.p is distinct from a.p order by 1 desc, b.sig;
```

**המדד האמיתי הוא היחס בין "נוקו" ל"נוצרו", לא הציון.** ציון יכול לרדת בגלל שינוי במכנה, ובגלל שהאתר נעשה ריק יותר.

**תמיד תפצל לפי ביטחון (`confidence`).** ערך 1 = בדיקה דטרמיניסטית בקוד, אמינה לחלוטין. ערך 0 = הסימן לא הוערך בכלל. ערכים 0.5–0.6 = המודל מנחש. בריצה האחרונה נוקו 26 סימנים, אבל רק 10 בביטחון גבוה. **תדווח לי את המספר הכשר, לא את המספר היפה.**

## מה נבנה בסשן האחרון (19.8.2026)

יום ארוך. מצאנו ותיקנו שש עשרה בעיות. יש סיכום ויזואלי מלא שכבר שיתפתי:
https://claude.ai/code/artifact/d2616a2f-ba2c-4ad3-8b6e-344f34cb3f78

### הבעיה הגדולה: המערכת מחקה תוכן

`rebuild` היה מבקש ממודל לרשום את תוכן הדף ל-JSON, ואז בונה רק מהרשימה וזורק את המקור. הרשימה יצאה חלקית — המודל מסכם במקום להעתיק. באתר אמיתי (ארסנל) **94 אחוז מהתוכן נמחק, והציון ירד מ-47 ל-32**, כלומר ההרס נראה כמו ההצלחה הכי גדולה שהיתה למערכת.

השותף שלי כתב את התיקון בענף נפרד; מיזגתי אותו. עכשיו התוכן **מחולץ דטרמיניסטית מה-DOM** (`_shared/ledger.ts`), והמודל מקבל רק את העיצוב. יש גם "רצפת שלמות" שמשלימה סקשן שהמודל החזיר חסר.

### שני שומרים חדשים

שתי הבעיות הגדולות של היום הן אותה מחלה: **הציון מודד רק מה רע, אז מחיקה נראית כמו הצלחה.**

- **`_shared/preservation.ts`** — משווה את הדף שיצא לדף שנכנס: כמות טקסט, כל מספר (מחיר, שנה, טלפון), וכל מזהה שהסקריפטים של האתר צריכים. מכויל על כל הריצות שבאחסון.
- **`_shared/richness.ts`** — מודד עומק עיצובי מול המקור בשש קטגוריות שאפשר להחליף ביניהן, ונותן לבנאי **יעד עיצובי מראש** שנגזר מהאתר שלו עצמו.

### לולאת בדיקה עצמית

**`_shared/selfcheck.ts`** — אחרי הבנייה, מריץ על התוצאה את אותו בודק דטרמיניסטי של האבחון, מתקן מה שאפשר בקוד, ומריץ שוב כדי **להוכיח** שהתיקון עבד. מתקן היום: אמוג'י (#109), מאפייני CSS פיזיים (#64), והחזרת מזהים שהסקריפטים צריכים. לא נוגע ב-CSS של ווידג'ט שנשמר מהמקור.

### עוד תיקונים

- בחירת דף הבית (היה לפי אלפבית, אז תמיד "צור קשר" ואף פעם לא index).
- סינון קבצים: `pax_global_header`, README, קבצי מק, **וכל קובץ שמתחיל בנקודה** — כולל `.env`, שעד אז נשלח למודל עם התוכן שלו.
- סקריפטים בקבצים חיצוניים: הדף הבנוי השאיר קישור ל-`script.js` ומחק את `#nav` ו-`#formNote` שהוא צריך. **שגיאה אדומה בכל גלילה**, אומת בדפדפן.
- הפוטר: איבד כתובת, שעות, ח״פ וקישורי תקנון. עכשיו כל שורות הפוטר עוברות לבנייה.
- מחיקת סריקה השאירה שלושה קבצים עם תוכן האתר באחסון. מוחקת עכשיו הכל.
- טקסט מודבק: `₪ 24,900לפרטים` ו-`אזור תעשייהראשון לציון`. תוקן בשורש לכל האתר.
- סימן #2 (פונט שחוק) עבר לבדיקה דטרמיניסטית. יש עכשיו **12 בדיקות כאלה** ב-`mechanical.ts`.
- חמישה סימנים לא נספרים יותר (36, 100, 41, 101, 106) — `NOT_COUNTED` ב-`catalogue.ts`.
- **16 סימנים מסומנים כ"מחכה לבעל האתר"** — `OWNER_INPUT` ב-`catalogue.ts`, מוצג בדשבורד.

### התוצאה האחרונה

אתר `maon-furniture-landing`, סריקה `1569e11e`:
- 45 סימנים ירדו ל-24. נוקו 26, נוצרו 5.
- עומק עיצובי: **113% מהמקורי** (מסע: 58% → 88% → 113%).
- תוכן: 381 מילים גדלו ל-467, אפס מספרים אבדו, אפס עוגני סקריפט אבדו.
- בדפדפן: **קונסול ריק לגמרי**, והסקריפט המקורי פועל.

## מה נשאר — לפי סדר עדיפות

**1. כיסוי רב־עמודי.** `rebuild` בונה עמוד אחד. מדדנו: זה נוגע לאתר אחד מתוך שלושים ושניים, ולכן דחינו במודע. כשתיגש לזה — יש שלוש אפשרויות שכבר נדונו: לבנות כל עמוד במלואו (יקר, בערך פי ארבעה), להלביש את העמודים המשניים בעיצוב החדש בלבד (זול, פותר בערך שליש), או להעביר אותם למסלול `transform` הקיים (לא מומלץ). **אל תתחיל לקודד לפני שתציג לי אפשרויות.**

**2. אי־עקביות קטנה בנתונים.** שניים מהחמישה שהוצאנו מהספירה (41, 106) עדיין רשומים `present: true` למרות ש-`applicable: false`. לא משפיע על שום מספר שמוצג, אבל מבלבל בנתונים הגולמיים.

**3. שני סימנים שנוצרו וטרם טופלו.** בריצה האחרונה נוצרו חמישה, שניים מהם בביטחון גבוה: #78 (כיוון מספרים) ו-#2 — **שניהם אמורים להיעלם בריצה הבאה** בזכות תיקוני היום, אבל זה טרם אומת על ריצה חיה.

**4. מגבלה קבועה.** 16 סימנים דורשים עובדה שרק בעל האתר יכול לתת — ח״פ, כתובת, תמונות אמיתיות, המלצות, חשבון מדידה. היעד הריאלי הוא 10–15 סימנים שנשארים, לא אפס.

## שיפורים לאלגוריתם — רעיונות שלא מומשו

אלה לא משימות פתוחות אלא כיוונים ששווה לשקול. אף אחד מהם לא נבדק.

- **להרחיב את לולאת הבדיקה העצמית.** היום היא מתקנת שני סימנים כי רק להם יש בודק דטרמיניסטי. כל סימן שיעבור ל-`mechanical.ts` הופך מיד למועמד לתיקון אוטומטי בחינם. מועמדים סבירים: #56 (focus outline), #61 (dataLayer), #99 (canonical).
- **להקטין את רשימת האיסורים.** הבנאי מקבל איסור אחד לכל סימן שנמצא — 45 בריצה אמיתית. זה מה שרוקן את העיצוב. אפשר לקבץ אותם לפי קטגוריה במקום אחד־אחד, ולראות אם התוצאה משתפרת.
- **לתת לרצפת העיצוב להזין את הריצה עצמה.** יש כבר `richnessBrief()` ב-`richness.ts` שמנסח בדיוק אילו קטגוריות יצאו דלות — הוא כתוב ומכוסה בבדיקות, אבל **לא מחובר לשום מקום**. אפשר להזין אותו לניסיון שני אוטומטי של שלב המעטפת כשהעומק נופל. זה מייקר ריצה, אז צריך להחליט.
- **לבדוק את המדידה של המדידה.** שלושה מדדים שכתבתי היו שגויים בסשן הזה, וכולם התגלו רק מול נתונים אמיתיים. שווה לבנות סקריפט קבוע שמריץ כל שומר על כל הריצות שבאחסון ומדווח, כדי שרגרסיה במדד תיתפס מיד.
- **לשקול לתת ציון גם על מה שטוב.** הציון סופר רק בעיות, וזה השורש של שתי התקלות הגדולות של היום. רצפת העיצוב היא תיקון נקודתי; מדד חיובי אמיתי הוא שינוי מוצר.

## כללי ברזל של הפרויקט

- **אף פעם לא ממציאים עובדות.** לא מספר חברה, לא טלפון, לא כתובת, לא תמונה. אם אין נתון — לא כותבים תגית.
- **אף פעם לא מסרבים למסור.** בנייה תמיד נמסרת עם כפתור הורדה. אם שומר מצא בעיה — היא מוצגת מעל הכפתור עם המספרים המדויקים, ואני מחליט. חסימה מחרימה עבודה ששילמתי עליה, וזה מוצדק רק אם אתה בטוח לגמרי — ומדד עקיף אף פעם לא בטוח לגמרי. זה נלמד בדרך הקשה: רצפת העיצוב חסמה בנייה תקינה לגמרי.
- **בדיקה דטרמיניסטית עדיפה על שאלה למודל**, בתנאי שהיא עונה לשני הכיוונים. במיוחד כשמדובר ברשימה סגורה — המודל סימן פונט כשחוק בזמן שההסבר שלו עצמו אמר שהוא לא ברשימה.
- **תשובה בטוחה ושגויה גרועה מתשובה מהוססת.** כל בדיקה חדשה ב-`mechanical.ts` חייבת בדיקות אוטומטיות בשני הכיוונים.
- **תקן בשורש, לא במקום שבו שמת לב לבאג.** תיקנתי הדבקת טקסט בפוטר בלבד, והבאג המשיך לחיות בכל שאר האתר עד שהוא צץ שוב במחירים.
- **תחשוד גם בכלי המדידה שלך.** שלושה מדדים שכתבתי היום היו שבורים: אחד לא הבחין בין דף עשיר לדף מת, אחד העניש קוד שעומד בכללים שלנו עצמנו, ואחד תגמל קוד מבולגן והעניש קוד מסודר.
- **אל תשנה את `SYSTEM_PROMPT` ב-`detect`** בלי סיבה טובה — הבייטים חייבים להישאר זהים כדי שהקאשינג יעבוד.
- UI חדש צריך לעבור את 110 הסימנים של המוצר עצמו. מרכוז רק לבלוקים בודדים, מאפייני CSS לוגיים, בלי אמוג'י.

## קבצים שכדאי לקרוא ראשונים

| קובץ | למה |
|---|---|
| `supabase/functions/_shared/ledger.ts` | חילוץ התוכן מה-DOM. הלב של המערכת מאז המיזוג |
| `supabase/functions/_shared/preservation.ts` | שומר התוכן — מה נחשב אובדן ולמה |
| `supabase/functions/_shared/richness.ts` | רצפת העיצוב, ושלושת הכשלים שלה שתוקנו |
| `supabase/functions/_shared/selfcheck.ts` | לולאת התיקון העצמי והכלל שתיקון חייב להיות מוכח |
| `supabase/functions/_shared/mechanical.ts` | 12 הבדיקות הדטרמיניסטיות והכללים לאילו סימנים מותר להעביר לשם |
| `supabase/functions/_shared/catalogue.ts` | הבטחת השלמות של 110 הסימנים, `NOT_COUNTED`, `OWNER_INPUT` |
| `supabase/functions/rebuild/index.ts` | הבנייה מאפס, כולל `NEVER_INTRODUCE`, `headMeta` ורצף השומרים בסוף |
| `supabase/functions/_shared/signals.json` | 110 הסימנים. **תמיד לקרוא את שדה `detection` ולא רק את השם** — טעות כזאת כבר עלתה לנו בתחזית שגויה |
| `js/app.js` | כל הדשבורד: סרגל ההתקדמות, בלוק "מה מחכה לך", בלוק אזהרות הבנייה |

**הערות הקוד הן התיעוד האמיתי.** כל מודול חדש פותח בהסבר למה הוא קיים, עם המספרים מהמקרה האמיתי שגרם לו להיכתב. אל תמחק אותן, ואם אתה משנה התנהגות — עדכן אותן.

התחל בכך שתאשר לי שקראת, ותגיד לי מה לדעתך הצעד הבא ולמה.
