# הפעלת סליקה עם Paddle

**כל הקוד כבר כתוב ונדחף לגיטהאב.** החנות מחוברת לצ'ק-אאוט אמיתי, יש פונקציית
webhook שמזכה את היתרה, והכל מכוסה בבדיקות (474 עוברות).

מה שנשאר זה החלק שאני לא יכול לעשות במקומך, כי הוא דורש את החשבון שלך:
להגדיר מוצר, אישור אתר, ו-webhook, ולתת לי חמישה ערכים.

עד שזה קורה, החנות אומרת "התשלום עדיין לא פעיל" ולא מנסה לגבות כלום.

> **למה Paddle ולא Lemon Squeezy.** נרשמת ב-Lemon Squeezy והם ניתבו אותך
> ל-Paddle — הם החברה האם, וטופס ההרשמה מנתב לפי גודל ואופי העסק. המחיר זהה
> בשניהם: **5% + 50 סנט לעסקה**. הקוד הומר ל-Paddle ב-2026-09-07.

---

## ההבדל שחשוב להבין לפני שמתחילים

ב-Paddle **חלון התשלום נפתח מעל עמוד החנות שלך**, לא באתר חיצוני. השרת יוצר
"עסקה", והדפדפן פותח עליה חלון בעזרת Paddle.js שכבר מוטמע בעמוד. הלקוח לא
עוזב את האתר בכלל.

זה אומר שני דברים מעשיים:

1. **חייב אישור אתר.** Paddle לא ייצרו עסקה בכלל עד שהאתר שלך מאושר אצלם
   ומוגדר כ-"default payment link". זה השלב שאנשים מפספסים והשגיאה שחוזרת
   לא מסבירה את עצמה.
2. **אין חזרה מדף תשלום.** ברגע שהתשלום מסתיים, החלון נסגר והעמוד מתחיל
   לחכות שהיתרה תעלה. אין ניווט, אין `?paid=1` במקרה הרגיל.

---

## מה אני צריך שתעשה — לפי הסדר

### שלב 1 — הישאר ב-Sandbox

למעלה במסך צריך לכתוב **"You're in Sandbox"**. אל תבנה מול Live: ב-Sandbox
אפשר לשלם בכרטיס בדיקה בלי אימות חשבון ובלי כסף אמיתי.

לכל סביבה יש מפתחות משלה ונתונים משלה. מפתח Sandbox מול Live לא יעבוד, וזו
הסיבה הנפוצה ביותר ל-"שילמתי ולא קרה כלום".

### שלב 2 — מוצר אחד (5 דקות)

**Catalog → Products → New Product**

- Name: `Tashaneh Tokens` — זה מה שהלקוח רואה בקבלה
- אם הוא דורש מחיר, תוסיף **one-time של $10**. זה placeholder בלבד: השרת
  שולח מחיר משלו בכל עסקה ("non-catalog price"), ולכן מוצר אחד מכסה את כל
  ארבע החבילות וגם כמות שהלקוח הקליד בעצמו.
- **העתק את ה-Product ID** — מתחיל ב-`pro_`.

### שלב 3 — אישור אתר + default payment link (5 דקות + המתנה)

**Checkout → Website approval** — הוסף את `www.teshane.com` ושלח לאישור.

אחר כך, באותו אזור, הגדר **default payment link**:

```
https://www.teshane.com/store/
```

ב-Sandbox מותר גם `http://localhost:5500/store/` לצורך בדיקות.

### שלב 4 — מפתחות (3 דקות)

**Developer tools → Authentication.** צריך **שניים**, והם לא אותו דבר:

| מה | תחילית | סודי? |
|---|---|---|
| **API key** | `pdl_sdbx_apikey_...` | **כן** — לשרת בלבד |
| **Client-side token** | `test_...` | לא — נועד לשבת בדף אינטרנט |

את ה-API key העתק **מיד**, הוא מוצג פעם אחת.

### שלב 5 — Webhook (3 דקות)

**Developer tools → Notifications → New destination**

- Type: **Webhook (URL)**
- URL: `https://iizcvlkktkbftbuvxlvw.supabase.co/functions/v1/paddle-webhook`
- Events: **`transaction.completed`** ו-**`adjustment.created`**. אלה השניים
  היחידים שמזיזים משהו אצלנו.
- אחרי השמירה — היכנס ל-destination והעתק את ה-**secret key**, מתחיל
  ב-`pdl_ntfset_`. **זה הדבר הכי רגיש בכל התהליך**: הוא מה שמוכיח שהפנייה
  באמת מ-Paddle ולא ממישהו שמצא את הכתובת.

---

## מה אני צריך שתיתן לי

חמישה ערכים. **תדביק אותם בעצמך** ב-Supabase — `Project Settings → Edge
Functions → Secrets → Add new secret` — בשמות המדויקים האלה, ואז תגיד לי
"הוספתי". מפתח שעובר בצ'אט הוא מפתח שצריך להחליף אחר כך.

| שם הסוד | מאיפה | דוגמה |
|---|---|---|
| `PADDLE_API_KEY` | Developer tools → Authentication | `pdl_sdbx_apikey_01j...` |
| `PADDLE_CLIENT_TOKEN` | אותו מסך, client-side token | `test_a1b2c3...` |
| `PADDLE_PRODUCT_ID` | Catalog → Products | `pro_01j...` |
| `PADDLE_WEBHOOK_SECRET` | Notifications → ה-destination | `pdl_ntfset_...` |
| `PADDLE_ENV` | הקלד ידנית | `sandbox` (ואחר כך `production`) |

בנוסף צריך `SITE_URL` — הכתובת הקנונית של האתר:

```
https://www.teshane.com
```

עם ה-`www`, בלי סלאש בסוף. בדקתי: `teshane.com` מפנה (308) ל-`www`, אז זו
הכתובת שהלקוח באמת יושב עליה.

---

## מה אני עושה אחרי שתגיד "הוספתי"

1. מריץ את המיגרציה `0014_purchases.sql` (טבלת הרכישות + הזיכוי החד-פעמי).
   היא עדיין לא רצה — הטבלה לא קיימת במסד.
2. פורס את שתי הפונקציות:
   ```bash
   npx --yes supabase@2.115.0 functions deploy checkout --project-ref iizcvlkktkbftbuvxlvw
   npx --yes supabase@2.115.0 functions deploy paddle-webhook --project-ref iizcvlkktkbftbuvxlvw --no-verify-jwt
   ```
   ה-`--no-verify-jwt` על ה-webhook הוא חובה: Paddle פונים לשם בלי טוקן
   התחברות. מה שמגן על הכתובת הוא בדיקת החתימה בתוך הפונקציה.
3. עושים יחד קנייה אחת ב-Sandbox עם כרטיס הבדיקה `4242 4242 4242 4242`
   (כל תאריך עתידי, כל CVC), ומוודאים שהיתרה עלתה.
4. אחרי שזה עובד: יוצרים מפתחות Live, מחליפים את חמשת הסודות,
   `PADDLE_ENV=production`, ופורסים שוב.

---

## מה לא בדוק, ומה קורה אם ייפול

את התמחור, אימות החתימה והזיכוי החד-פעמי בדקתי ב-474 בדיקות. את **החיבור
ל-Paddle עצמו לא הרצתי** — אין מפתחות. שלושה דברים רק עסקה אמיתית תוכיח:

1. שהצורה של בקשת יצירת העסקה מתקבלת (בניתי אותה מול התיעוד שלהם).
2. שה-`custom_data` חוזר ב-webhook — שם יושבים ה-user_id והכמות, וכל הזיכוי
   תלוי בזה.
3. שהחלון נפתח על העמוד כמו שצריך.

**אופן הכישלון בטוח:** אם משהו בבקשה לא נכון, הלקוח מקבל „לא הצלחנו לפתוח דף
תשלום. לא חויבת“ ושום כסף לא זז. הכיוון המסוכן — כסף שעובר בלי טוקנים —
מכוסה מהצד השני: Paddle שולחים את ה-webhook שוב ושוב עד שהם מקבלים תשובה
תקינה, אז גם אם הזיכוי נכשל בפעם הראשונה הוא יקרה.

---

## שלוש החלטות שכדאי לקבל לפני Live

### 1. המינימום בשדה „כמות משלכם“

העמלה היא 5% **+ 50 סנט קבועים**, וה-50 סנט הם מה שקובע בעסקאות קטנות:

| עסקה | עמלה | אחוז בפועל | נטו |
|---|---|---|---|
| $1 (טוקן בודד) | $0.55 | **55%** | $0.45 |
| $10 (10 טוקנים) | $1.00 | 10.0% | $9.00 |
| $18 (20 טוקנים) | $1.40 | 7.8% | $16.60 |
| $42 (50 טוקנים) | $2.60 | 6.2% | $39.40 |
| $80 (100 טוקנים) | $4.50 | 5.6% | $75.50 |

כרגע השדה מאפשר לקנות טוקן אחד ב-$1, ועל זה Paddle לוקחים 55%. גם בדף
המחירים שלהם כתוב במפורש שמכירות מתחת ל-$10 דורשות תמחור מותאם. סריקה +
בנייה עולות לך בערך $2.0–2.3, אז עסקה של $1 היא הפסד ודאי.

**ההצעה:** להעלות את המינימום ל-**10 טוקנים** — כמו החבילה הקטנה, וכמו הסף
של Paddle. אז כל עסקה אפשרית היא לפחות $10 והעמלה נעצרת על 10%. שינוי של
דקה בשני מקומות (`js/store.js` ו-`_shared/pricing.ts`), אבל זו החלטת תמחור
ולא אגע בה בלי בקשה.

### 2. החזרים

המערכת **רושמת** החזר אבל לא מורידה טוקנים אוטומטית. עד שההחזר מגיע הלקוח
כבר יכול היה לנצל את הטוקנים, וקיזוז עיוור או מתנגש עם המגבלה שיתרה לא יורדת
מתחת לאפס, או משאיר לקוח באמצע בנייה בלי טוקנים ובלי הסבר. ההחזר מופיע
ב-`purchase_log` ואתה מחליט ידנית.

### 3. מדיניות ביטולים ותנאי שימוש

Paddle דורשים קישור למדיניות החזרים בעמוד התשלום ובאתר. אם אין לך עמוד כזה,
זה חוסם את אישור האתר ואת המעבר ל-Live. שווה גם לשאול רואה חשבון לגבי
חשבוניות ישראליות — Paddle מוציאים קבלה ללקוח, לא חשבונית בשמך.

---

## מה כבר עובד, בלי שתעשה כלום

- **המחיר לא נקבע בדפדפן.** העמוד שולח כמות בלבד; השרת מתמחר אותה מהעותק
  שלו ומזהה את הקונה מה-JWT. לקוח שיערוך את העמוד אצלו יכול להזמין כמות
  אחרת — במחיר האמיתי של אותה כמות, לחשבון שלו. זה הכל.
  `pricing.test.ts` קורא את `js/store.js` ונופל אם העמוד והשרת מתמחרים אחרת.
- **תשלום מזכה פעם אחת בדיוק.** הזיכוי נעול על מזהה העסקה של Paddle, והפנייה
  השנייה לא מוסיפה כלום.
- **החתימה נבדקת על הבייטים הגולמיים** לפני פענוח, בהשוואה בזמן קבוע, עם
  חלון זמן של 5 דקות כדי שגוף שנתפס פעם אחת לא יעבוד לנצח.
- **הכפתור ננעל בזמן הפנייה.** לחיצה שנייה היא עסקה שנייה.
- **סגירת החלון בלי לשלם** מחזירה את הכפתורים בשקט — לא קרה כלום, אז לא
  נאמר כלום.
- **אחרי תשלום** העמוד מחכה ליתרה שהוא מצפה לה ואומר כמה טוקנים נוספו וכמה
  יש עכשיו. אם ה-webhook מתעכב, הוא אומר שהתשלום נרשם — כי הוא נרשם.
