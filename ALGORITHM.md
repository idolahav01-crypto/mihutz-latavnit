# איך אנחנו משנים את האתר — שידרוג "במקום" (branch: `transform-in-place-redesign`)

**הרעיון בשורה אחת:** **לא זורקים** את הקוד המקורי — לוקחים את אותו שלד DOM (כל
class/id/handler/script), **מארגנים אותו מחדש** נועז, ומחליפים לגמרי את הסגנון.

> אלגוריתם השינוי השני שלנו — "בנייה מחדש מאפס" (rebuild) — נמצא בבראנץ' `main`.
> ההבדל ביניהם בתחתית הקובץ.

---

## אלגוריתם השינוי (מדויק, שלב-שלב)

**קלט:** קבצי המקור + תוצאת ה-detect.

יש שני מסלולים; המסלול הראשי הוא TransformDesigner.

### מסלול ראשי — TransformDesigner (עריכת-על של קובץ שלם)
פונקציה: `transform`. **לכל קובץ HTML, שתי קריאות** (כל אחת 150 שניות משלה):

**A. phase "structure" — משכתבים את ה-`<body>`:**
- מרכיבים מחדש נועז את ה-markup (hero חדש, סדר סקשנים חדש, wrappers חדשים).
- **משמרים בדיוק:** כל class / id / data-attr / handler (onclick…) / route / href + כל
  העובדות + placeholders של ה-`<script>`. מותר להוסיף wrappers ו-classes חדשים.
- שולפים את ה-`<script>` מראש ומחזירים byte-for-byte (המודל לא רואה/כותב JS).

**B. phase "css" — מייצרים גיליון סגנון חדש שלם:**
- CSS חדש לגמרי שמכוון לסלקטורים שקיימים ב-markha (ה-classes/ids ששרדו) + קישורי פונטים.
- `design_direction` ננעל בקובץ הראשון ומשוחזר בשאר לקוהרנטיות.

קובץ `.css`/`.scss` = קריאת `cssfile` אחת (שכתוב CSS מלא).
**הרכבה:** שומרים את ה-`<head>`/פתיחת ה-`<body>` המקוריים, מחליפים את תוכן ה-`<body>`,
מחליפים את בלוק ה-`<style>`, מחזירים את ה-scripts.

### מסלול משני — Design → Apply → QA (הטלאות לפי סימן)
1. **`design`** — לכל סימן present מפיק זוג `old_code` (ציטוט מילולי ייחודי מהקוד) → `new_code` (drop-in מלא).
2. **`apply`** — **דטרמיניסטי, בלי מודל**: מחליף כל `old_code`→`new_code` בהחלפת מחרוזת מדויקת, בסדר תלויות.
3. **`qa`** — משווה מול המקור לפאריטי פונקציונלי + דורש שינוי נראה לעין.

**פלט:** אותו שלד DOM (כל ה-classes/ids שרדו) מאורגן מחדש + סגנון חדש לגמרי.

---

## חוזי-הברזל של המסלול הזה
- **שימור מוחלט:** class / id / data-attr / handler / route + כל העובדות + ה-`<script>`.
- **עוגן מילולי:** במסלול ההטלאות, כל fix חייב `old_code` שהוא substring ייחודי — אחרת נדחה.

## מגבלות ידועות (הסיבה שבנינו את rebuild ב-main)
- **SPA (React/Next):** כמעט אין markup סטטי לשכתוב; ה-CSS מכוון לסלקטורים שנוצרים רק בזמן ריצה.
- **CSS חיצוני / Tailwind:** רק בלוק ה-`<style>` הראשון מוחלף; ה-utilities מנצחים.
- **דפים גדולים:** חיתוך פלט (maxTokens). **markup לא-סטנדרטי:** `splitBody` נכשל.

---

## ההבדל מ-`main` (חשוב!)

| | **שידרוג במקום** (כאן) | **בנייה מאפס** (main) |
|---|---|---|
| ה-markup המקורי | **נשמר** ומאורגן מחדש | **נזרק** ומיוצר מחדש |
| class/id/handlers מהמקור | **שורדים במלואם** | **לא שורדים** (DOM חדש) |
| מקור המבנה של הפלט | המבנה של **המקור**, משודרג | המבנה **שלנו** |
| מתי להשתמש | כשחייבים לשמר בדיוק את חיווט ה-class/id/JS | כשרוצים אתר חדש באמת |
| חולשה | SPA / CSS חיצוני / דפים גדולים | — |

בקצרה: **transform עורך את מה שקיים. main מבין ואז מייצר מחדש.**
