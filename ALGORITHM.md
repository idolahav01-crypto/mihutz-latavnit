# איך אנחנו משנים את האתר — בנייה מחדש מאפס (branch: `main`)

**הרעיון בשורה אחת:** זורקים את ה-markup המקורי לגמרי, מבינים את האתר כ**דאטה**,
ומייצרים אתר חדש לגמרי בפורמט שאנחנו שולטים בו.

> אלגוריתם השינוי השני שלנו — "שידרוג במקום" (transform) — נמצא בבראנץ'
> `transform-in-place-redesign`. ההבדל ביניהם בתחתית הקובץ.

---

## אלגוריתם השינוי (מדויק, שלב-שלב)

**קלט:** קבצי המקור + תוצאת ה-detect.

**1. חילוץ מודל תוכן (`spec`) — הופכים את האתר לדאטה, לא markup:**
- `meta{שם, מטרה, שפה, קהל, טון}`, `dir` (rtl/ltr)
- `facts[]` — **כל העובדות האמיתיות** (מספרים, מחירים, שמות, תאריכים, יצירת קשר).
  זהו **חוזה**: כל שלבי הייצור רשאים להשתמש **אך ורק** בעובדות שכאן. ← זה מה שמונע המצאת תוכן.
- `sections[]` — לכל סקשן: `type, heading, body, items[], cta, component_id`
- `components[]` — רכיבים אינטראקטיביים: `container_id` + אינדקס ה-`<script>` שמפעיל אותם
- **מנקים כל אמוג'י** מכל טקסט.
- שולפים את בלוקי ה-`<script>` ושומרים byte-for-byte (המודל לעולם לא רואה/מייצר JS).
- מפיקים `design_direction{palette, typography, layout_principle, personality}`.

**2. ייצור ה-Shell מתוך ה-design_direction:**
`tokens_css` (משתני CSS, type scale, מרווחים, מחלקות רכיבים) + קישורי פונטים + `header` + `footer`.

**3. לכל סקשן ב-spec — ייצור מאפס (קריאה נפרדת לכל אחד):**
`html` + `css` חדשים לגמרי, תוך שימוש **רק** בעובדות של אותו סקשן + הטוקנים מה-Shell.

**4. הרכבה דטרמיניסטית:**
מסמך `<!doctype>` חדש לגמרי = `<head>`(meta + פונטים + `tokens_css` + כל ה-CSS של הסקשנים)
+ `<body>`(header + הסקשנים + footer + ה-`<script>` שהוחזרו).

**פלט:** מסמך ש**שום דבר מה-HTML/CSS המקורי לא שורד בו** — רק העובדות, נתיבי הנכסים, וה-scripts.

---

## למה זה יוצא אחיד (הערובות)
- **פורמט משלנו** → הפונטים תמיד נטענים, אפס קלישאות AI (מוטמע במחולל), תמיד מתרנדר.
- **חוזה העובדות** → אי אפשר להמציא או להשמיט תוכן.
- **אפס אמוג'י** (סימן #109) → אסור לחלוטין, בכל מקום.
- **שימור פונקציונליות** → ה-`<script>` המקורי נישא ומחובר לפי `id`.

## מדידת "אחרי" (כן ומדיד)
אחרי שמשנים, מריצים `detect` שוב עם `mode:"after"` על האתר החדש — **אותה נוסחת ציון בדיוק** —
ומציגים **לפני → אחרי** של כמה האתר AI (0=אנושי, 100=AI).

---

## ההבדל מ-`transform-in-place-redesign` (חשוב!)

| | **בנייה מאפס** (main) | **שידרוג במקום** (transform-in-place) |
|---|---|---|
| ה-markup המקורי | **נזרק** ומיוצר מחדש | **נשמר** ומאורגן מחדש |
| class/id/handlers מהמקור | **לא שורדים** (DOM חדש משלנו) | **שורדים במלואם** |
| מקור המבנה של הפלט | המבנה **שלנו** | המבנה של **המקור**, משודרג |
| מתי להשתמש | כשרוצים אתר חדש באמת | כשחייבים לשמר בדיוק את חיווט ה-class/id/JS |
| סיכון | פחות תלוי בסוג האתר | נחלש על SPA / CSS חיצוני / דפים גדולים |

בקצרה: **main מבין ואז מייצר מחדש. transform עורך את מה שקיים.**
