# מסירה לסשן חדש — מחוץ לתבנית

העתק את כל הקובץ הזה כהודעה ראשונה בסשן חדש.

---

אתה ממשיך עבודה על הפרויקט הזה במקום סשן קודם. קרא את כל המסמך לפני שאתה עושה משהו.

## דבר ראשון — איך לדבר איתי

- ענה **בעברית פשוטה וברורה**, בלי מושגים טכניים מיותרים ובלי ז'רגון. כשאתה חייב מונח טכני, הסבר אותו במילה.
- **בלי אמוג'י** בשום מקום — לא בתשובות ולא בקוד. זה גם אחד מ-110 הסימנים שהמוצר עצמו מחפש.
- אל תגיד לי להריץ סריקה או בנייה **עד שאתה בטוח במאה אחוז** שהקוד שכתבת באמת רץ. כל ריצה עולה כסף אמיתי. הסשן הקודם הבטיח לי שיפור על סמך קוד שלא בדק שהוא פועל, והתברר שהוא נפל בשקט.
- כשאתה לא בטוח — תגיד את זה במפורש, ותפריד בין מה שמובטח (קוד דטרמיניסטי עם בדיקות) לבין מה שתלוי בהתנהגות של מודל.
- אל תסתפק בקריאת קוד. **תמדוד מול הנתונים האמיתיים** במסד לפני שאתה מסיק מסקנה.

## מה המוצר עושה

אתר בעברית ובאנגלית שמקבל קוד מקור של אתר, מאבחן אותו מול **110 "סימני AI"** (טביעות אצבע של אתרים שנבנו במחוללים אוטומטיים), נותן ציון, ואז יודע לבנות את האתר מחדש מאפס בלי הסימנים האלה.

- Frontend: HTML/CSS/JS סטטי, ללא framework. הדשבורד ב-`he/app/` ו-`app/`, כל הלוגיקה ב-`js/app.js`.
- Backend: Supabase Edge Functions ב-Deno, תחת `supabase/functions/`.
- אירוח: Vercel, פריסה אוטומטית מ-`main` בגיטהאב.
- מודלים: Sonnet 5 לאבחון, Opus 4.8 לבנייה, דרך `_shared/anthropic.ts`.

השלבים המרכזיים: `detect` (אבחון), `rebuild` (בנייה מאפס), `features` (הצעת פיצ'ר ובנייתו), `package` (הורדת zip), `push-github` (פתיחת Pull Request).

## דברים תפעוליים שחייבים לדעת

**פריסה לשרת עובדת מהטרמינל.** ה-CLI כבר מחובר, יש אסימון שמור. אל תבקש ממני סיסמה או מפתח — צרור המפתחות במק שלי קורס, אבל זה חוסם רק התחברות מחדש, לא את האסימון הקיים.

```bash
npx --no-install supabase functions deploy <name> --project-ref iizcvlkktkbftbuvxlvw
```

מזהה הפרויקט: `iizcvlkktkbftbuvxlvw`. יש גם מחבר MCP ל-Supabase לשאילתות SQL ולוגים.

**חשוב:** `_shared/` נארז לתוך כל פונקציה בנפרד. אם שינית קובץ ב-`_shared/`, צריך לפרוס מחדש **את כל הפונקציות שמייבאות אותו** — לא רק אחת.

**בדיקות:**
```bash
npx --no-install deno test --allow-read --allow-net supabase/functions/_shared/
npx --no-install deno check supabase/functions/<name>/index.ts
```
כרגע 113 בדיקות עוברות. אל תפרוס בלי להריץ אותן.

**בדיקה בדפדפן:** תמיד לטעון מחדש עם `fetch(url, {cache:'reload'})` לפני רענון — הדפדפן שמר גרסאות ישנות של `js/app.js` וזה בזבז לנו זמן.

**יש פרצת פיתוח מכוונת** בשם `DEV_NO_AUTH` ב-`js/app.js`, שעוקפת את מסך ההתחברות. היא עובדת רק על localhost. השארתי אותה בכוונה — אל תמחק.

**אין לך גישה להריץ סריקה או בנייה בעצמך** — הן דורשות התחברות שלי דרך הדשבורד. אתה יכול לקרוא כל תוצאה במסד, ולהוריד את קבצי הבאנדל מהאחסון:
```bash
npx --no-install supabase storage ls "ss:///scans/<user_id>/<scan_id>/" --experimental
npx --no-install supabase storage cp "ss:///scans/<...>/bundle.txt" ./local.txt --experimental
```
זה מאפשר לך להוכיח דברים בלי לבזבז כסף. תשתמש בזה.

## איך למדוד תוצאות

הכל בטבלה `scans`. עמודות חשובות: `detection` (האבחון לפני), `detection_after` (אחרי הבנייה), `ai_fingerprint_score`, `present_count`, ואותם שניים עם סיומת `_after`, `stage_usage` (עלות וזמן לכל שלב), `site_url`.

השאילתה הכי שימושית — מה הבנייה ניקתה מול מה שהיא יצרה:

```sql
with b as (select (x->>'id')::int sig, x->>'name' nm, (x->>'present')::bool p
           from scans, jsonb_array_elements(detection->'signals') x where left(id::text,8)='XXXXXXXX'),
     a as (select (x->>'id')::int sig, (x->>'present')::bool p, (x->>'confidence')::float c
           from scans, jsonb_array_elements(detection_after->'signals') x where left(id::text,8)='XXXXXXXX')
select case when b.p and not a.p then 'CLEARED' else 'INTRODUCED' end kind, b.sig, b.nm, a.c
from b join a using (sig) where b.p is distinct from a.p order by 1 desc, b.sig;
```

**המדד האמיתי הוא היחס בין "נוקו" ל"נוצרו", לא הציון.** ציון יכול לרדת בגלל שינוי במכנה.

**שים לב לביטחון (`confidence`).** ערך 1 = בדיקה דטרמיניסטית בקוד, אמינה לחלוטין. ערך 0 = הסימן לא הוערך בכלל. ערכים 0.5–0.6 = המודל מנחש, ואל תבנה על זה מסקנה.

## מה נבנה ביום העבודה האחרון (14.8.2026)

### הבעיה שחקרנו
חלק מהאתרים הגיבו לבנייה מחדש וחלק כמעט לא. נמצאו ארבע סיבות נפרדות:

1. **הבנאי היה עיוור.** רשימת הסימנים נשלחה רק לשלב שבונה את המעטפת, לא לשלב שבונה את רוב הדף. תוקן. המדידה: מאפס סימנים שנוקו לשישה עד שלושה־עשר.
2. **רק קובץ אחד נכתב.** `rebuild` כותב את `htmlFiles[0]` בלבד. **עדיין פתוח.**
3. **לא היה קאשינג בשלבי הבנייה.** ההוראות הקבועות היו קצרות מהמינימום. הועברה רשימת האיסורים לתוכן, וזה גם פתר את בעיה 1 וגם הוזיל.
4. **רשימת האיסורים נגזרה רק ממה שהאתר המקורי נכשל בו** — ולכן דווקא מה שהוא עשה נכון נשבר. נוסף בלוק `NEVER_INTRODUCE` קבוע, ושלושה דברים הפכו לקוד דטרמיניסטי: skip link, `prefers-reduced-motion`, preload לפונט.

### כלי המדידה עצמו היה שבור
- חמש סריקות על קוד זהה נתנו 25–28 סימנים. 18 סימנים היו יציבים, 18 התנדנדו.
- **הפתרון:** `_shared/mechanical.ts` — 11 סימנים (1, 27, 30, 31, 40, 41, 45, 64, 94, 98, 109) עברו מהמודל לחיפוש טקסט בקוד. אותה תשובה תמיד, ביטחון 1. נמדד שלוש פעמים: אפס הבדלים.
- **באג שני:** המודל היה מפסיק באמצע ולא מחזיר את סוף הרשימה (93 ו-88 מתוך 110), והחישוב התעלם מהחסרים במקום לספור אותם. הפתרון: `_shared/catalogue.ts` שמוודא שכל 110 תמיד נספרים, plus מעבר השלמה עצמאי, plus פיצול האבחון לארבעה מעברים במקום שלושה. מאז: 110 סימנים, אפס לא נבדקו.

### התוצאה האחרונה
אתר `maon-furniture-landing`, סריקה `d71557fa`:
- לפני: 46 סימנים, ציון 49. אחרי: 33 סימנים, ציון 33.
- נוקו 17, נוצרו 4. היחס הכי טוב שנמדד (קודם: 11–15 מול 7–10).
- עלות: סריקה 0.34–0.52 דולר, בנייה מלאה 1.44–1.90 דולר.

## מה נשאר — לפי סדר עדיפות

**1. לאשר שמחיקת הקבצים המתים עובדת בפועל.** הקוד פרוס אבל טרם רץ בבנייה אמיתית.
הבנייה כותבת דף עצמאי עם CSS מוטמע, ולכן קובץ העיצוב הישן מתייתם וממשיך להישלח ונספר לרעתנו. נוסף `unreferencedAssets` ו-`DELETED_FILE` ב-`_shared/pipeline.ts`.
**כבר הוכח בסימולציה על הקבצים האמיתיים מהאחסון:** `style.css` נמחק וסימן #64 נעלם. חסרה רק בנייה אמיתית לאישור מקצה לקצה, ובה גם נראה אם #77 נעלם (הוא תלוי בשיפוט של מודל).

**2. כיסוי רב־עמודי (הבעיה הגדולה שנשארה).** `rebuild` כותב עמוד אחד. צריך תכנון: האם לבנות כל עמוד בנפרד עם מעטפת משותפת, או להלביש את העמודים המשניים במשתני העיצוב החדשים, או להעביר אותם למסלול `transform` הקיים. **אל תתחיל לקודד לפני שתציג לי אפשרויות.**

**3. לולאת בדיקה עצמית.** לסרוק את מה שנבנה ולתקן מה שנשבר. נדחה בכוונה עד שהמדידה תהיה אמינה — עכשיו היא כן, אז זה בשל.

**4. באג קטן: קבצי זבל נסרקים.** בבאנדל של סריקה אמיתית נמצא `pax_global_header` (שארית מחילוץ ארכיון) וגם `README.md`, ושניהם נספרים כקבצים. בעבר נמצא גם `._contact.html` של מק שקיבל שמונה ממצאים. צריך לסנן `^\._`, `__MACOSX/` ו-`pax_global_header` ברשימות הסינון ב-`js/app.js` וב-`fetch-repo`.

**5. תמונת שיתוף כשאין תמונות.** האתר האחרון בנוי כולו בגרדיאנטים, בלי אף `<img>`. אין תמונה אמיתית ל-og:image, ולכן סימן #31 יישאר דלוק. צריך להחליט אם זה מקובל. **אסור להמציא תמונה.**

**6. מגבלה קבועה.** 31 מתוך 110 הסימנים אינם ניתנים לתיקון אוטומטי — רובם רגולציה ישראלית שדורשת עובדות אמיתיות (ח״פ, תקנון, מדיניות ביטולים). היעד הריאלי הוא 10–15 סימנים שנשארים, לא אפס.

## כללי ברזל של הפרויקט

- **אף פעם לא ממציאים עובדות.** לא מספר חברה, לא טלפון, לא כתובת, לא תמונה. אם אין נתון — לא כותבים תגית.
- **בדיקה דטרמיניסטית עדיפה על שאלה למודל**, בתנאי שהיא עונה לשני הכיוונים. בדיקה שיודעת רק להוכיח נוכחות לא מייצבת כלום.
- **תשובה בטוחה ושגויה גרועה מתשובה מהוססת.** לכן כל בדיקה חדשה ב-`mechanical.ts` חייבת בדיקות אוטומטיות בשני הכיוונים.
- **אל תשנה את `SYSTEM_PROMPT` ב-`detect`** בלי סיבה טובה — הבייטים חייבים להישאר זהים כדי שהקאשינג יעבוד.
- UI חדש צריך לעבור את 110 הסימנים של המוצר עצמו. מרכוז רק לבלוקים בודדים, מאפייני CSS לוגיים, בלי אמוג'י.

## קבצים שכדאי לקרוא ראשונים

| קובץ | למה |
|---|---|
| `supabase/functions/_shared/mechanical.ts` | 11 הבדיקות הדטרמיניסטיות והכללים לאילו סימנים מותר להעביר לשם |
| `supabase/functions/_shared/catalogue.ts` | הבטחת השלמות של 110 הסימנים |
| `supabase/functions/rebuild/index.ts` | הבנייה מאפס, כולל `NEVER_INTRODUCE`, `headMeta`, `assemble` ומחיקת קבצים מתים |
| `supabase/functions/detect/index.ts` | האבחון, פיצול המעברים ומעבר ההשלמה |
| `supabase/functions/_shared/signals.json` | 110 הסימנים. **תמיד לקרוא את שדה `detection` ולא רק את השם** — טעות כזאת כבר עלתה לנו בתחזית שגויה |
| `js/app.js` | כל הדשבורד: סרגל ההתקדמות, זרימת הפיצ'ר, שדה הכתובת |

התחל בכך שתאשר לי שקראת, ותגיד לי מה לדעתך הצעד הבא ולמה.
