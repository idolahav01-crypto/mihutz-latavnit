# מסירה לסשן בדיקה — לוודא שחמש הבעיות באמת נסגרו

המטרה של הסשן הזה היא **לאמת, לא לתקן**. חמש בעיות נפתרו בסשן קודם, ובמקביל עבד
סשן אחר על בעיות אחרות באותם קבצים. השאלה היא אם החמש עדיין עומדות אחרי שהשני נגע
בהן — ולא רק אם הקוד קיים, אלא אם הוא באמת רץ.

**אל תריץ בנייה אמיתית עד הסוף.** בנייה עולה בערך דולר וחצי עד שניים. כל מה שכתוב
כאן נבדק בחינם. ריצה אמיתית היא הסעיף האחרון, ורק אם כל השאר ירוק.

---

## מצב נקודת הפתיחה

- ענף `main`, קומיט `91dd401`. מקומי ומרוחק זהים, עץ העבודה נקי.
- פונקציות סופאבייס נפרסו מה-HEAD הזה. וורסל נפרס ממנו גם — בדקתי, ה-hash של
  `js/app.js` החי זהה למקומי.
- **ארבע פונקציות מתות עדיין חיות בשרת**: `transform`, `design`, `apply`, `qa`.
  הן נמחקו מהקוד ואף כפתור לא פונה אליהן. לא נמחקו מהשרת בכוונה — מחיקה בלתי הפיכה.
  אם אתה מאמת ומוצא שהכל נקי, זה הרגע להציע מחיקה.
- מיגרציה `0006_cost_breakdown.sql` יושבת בקוד ו**לא הורצה** על מסד הנתונים.

הרצת הבדיקות (שים לב להרשאת הרשת — בלעדיה ארבע בדיקות נופלות ונראות כמו קוד שבור):

```bash
deno task test
```

---

## חמש הבעיות, ואיך לאמת כל אחת

### 1. CSS שבור בולע את שאר העמוד

**מה הייתה הבעיה.** הבנאי החזיר לפעמים CSS עם סוגר מסולסל חסר. סוגר חסר לא שובר
כלל אחד — הוא בולע את כל הכללים שאחריו עד סוף הקובץ. וזה קרה גם בנתיב הכשל: כשרצפת
ההשלמות מחליפה את ה-HTML של המודל, ה-CSS שלו עדיין נדבק לתוצאה.

**מה נעשה.** `balanceCss` ב-`_shared/selfcheck.ts` — סופרת סוגריים תוך דילוג על
הערות ומחרוזות, סוגרת מה שנשאר פתוח, זורקת סוגר שלא סוגר כלום.

**איפה לוודא שהיא באמת רצה** — זה החלק שקל לפספס:
- `rebuild/index.ts`, בלולאת הסקשנים: היא חייבת לרוץ על `built.css` **לפני**
  ההחלטה אם לקחת את ה-HTML של המודל או את הרצפה, ו-`sectionCss` חייב לקבל את
  התוצאה המאוזנת בשני הענפים.
- `selfcheck.ts`, בתוך `selfCheck`: מעבר שני על בלוקי ה-`<style>` המורכבים.

**בדיקה בחינם:** יש 11 בדיקות ב-`selfcheck.test.ts` תחת `balanceCss`. תקרא אותן
ותוודא שהן בודקות את הדבר הנכון, לא רק שהן עוברות. במיוחד: סוגר חסר שכללים אחריו
שורדים, סוגר בתוך מחרוזת `content:`, סוגר בתוך הערה, ו-CSS תקין שיוצא בייט-בבייט זהה.

**מה שצריך להישאר נכון:** התוצאה **תמיד** נמסרת. האזהרה `css_repaired` נוסעת לצד
ההורדה, לא במקומה. אם מצאת נתיב שבו CSS שבור עוצר מסירה — זו רגרסיה.

### 2. Path traversal בקבצי ZIP

**מה הייתה הבעיה, ומה לא.** התברר שכמעט סגור כבר: כלל הנקודה ב-`keepPath` מוריד
`..` כתופעת לוואי של הורדת `.git` ו-`.env`. החורים שנשארו: נתיב מוחלט, מפריד
backslash, ותוכן קובץ שמכיל שורת כותרת של פורמט החבילה ולכן יכול להמציא נתיב.

**מה נעשה.** `safeRelPath` ב-`_shared/pipeline.ts`, עותק זהה ב-`js/app.js`.
שלושה שערים: `keepPath`, `parseBundle`, ובניית ה-ZIP להורדה.

**הנקודה הכי חשובה לאמת:** הנתיב **שנשמר** הוא המנוקה, לא הגולמי. `keepPath` רק
מחליט אם לשאת קובץ, לא איך הוא נקרא. תוודא שזה נכון בשלושת המקומות שכותבים נתיב:
שני הבנדלרים ב-`js/app.js` ו-`fetch-repo/index.ts`.

**בדיקה בחינם:** `pipeline.test.ts`, הבלוק תחת "Path safety". תוסיף מקרה משלך אם
אתה חושב על צורה שלא כוסתה.

### 3. קבצי secrets

**מה הייתה הבעיה, ומה לא.** ההנחה המקורית הייתה ש-`.env` נשלח למודל. הוא לא, וכבר
מזמן. הבעיה האמיתית הייתה הפוכה: הוא גם לא **חזר** למשתמש. סוננו אותו מהחבילה, ולכן
הוא נעלם מההורדה — כלומר מחקנו למישהו את ההגדרות שלו.

**מה נעשה.** `SKIP_SECRETS` מוסיף את השמות שכלל הנקודה מפספס. והדפדפן שומר עותק
משלו בזיכרון, כבייטים, ומחזיר אותו ל-ZIP.

**מה לאמת:**
- הקבצים האלה **לא עולים לשרת בכלל**. אם מצאת נתיב שבו הם כן — זו הבעיה החמורה
  ביותר ברשימה הזאת.
- הם חוזרים ל-ZIP כבייטים מקוריים, לא כטקסט מפוענח. קובץ בינארי כמו `.p12` חייב
  לחזור תקין.
- הם **אחרונים** ב-ZIP ולא דורסים קובץ שהשרת ייצר.
- קובץ הגדרות רגיל כמו `config.json` או `package.json` **לא** נחשב סוד. יש בדיקה
  ייעודית לזה — אל תסמוך עליה, תקרא את הרגקס.

**מגבלה מוצהרת, לא באג:** זה עובד רק בסשן שבו העלית. הורדה מההיסטוריה לא תכיל
אותם, כי הם מעולם לא הגיעו אלינו. ההערה `#secrets-note` בדשבורד מופיעה רק כשהדפדפן
באמת מחזיק קבצים כאלה.

### 4 ו-5. שני מסלולי בנייה מתים נמחקו

`transform`, `design`, `apply`, `qa` וכל הקוד המשותף שנשאר בלי קורא — כ-3,171 שורות.
נשאר מסלול אחד: `rebuild`.

**התופעה שדרשה תיקון, וזה הסעיף שהכי חשוב לאמת:** `push-github` סירב לפתוח PR בלי
אישור מ-`qa`. עם `qa` מחוקה אף ריצה לא הייתה כותבת אישור כזה, ולכן פתיחת PR הייתה
נחסמת לתמיד. השער עבר להסתמך על סטטוס הריצה ועל ממצאי שני השומרים.

**ההתנהגות המדויקת שצריכה להתקיים:**

| מצב | הורדה | PR |
|---|---|---|
| בנייה נקייה | עובדת | נפתח מיד |
| `css_repaired` בלבד | עובדת | **נפתח מיד**, הממצא רק מדווח |
| `content_loss` או `design_thin` | עובדת | 409, מסך אישור, ואז נפתח |
| הריצה לא הגיעה ל-`applied` | לא רלוונטי | 409 |

`css_repaired` פטור בכוונה: הוא מתאר תקלה **שכבר תוקנה** לפני שהקוד נשלח, אז אין
בקומיט מה למנוע ממנו. הוא מופיע בגוף ה-PR ובדוח.

**ההורדה לא נחסמת בשום מקרה.** `package` לא מסתכל על הממצאים בכלל, ו-`rebuild` כותב
`applied` בלי קשר לאזהרות. אם מצאת נתיב שחוסם הורדה — זו רגרסיה מול כלל הברזל.

---

## מה עלול להיות שבור בגלל הסשן המקביל

שני סשנים נגעו באותם קבצים. הקומיטים `5197e24` ו-`0f3543f` שינו את
`js/app.js`, `pipeline.ts`, `fetch-repo/index.ts` ו-`rebuild/index.ts` — בדיוק
הקבצים של חמש הבעיות. הבדיקות עוברות (296), אבל בדיקות ירוקות לא מוכיחות שהתנהגות
לא השתנתה. שווה מבט ידני על:

- **הסינון בדפדפן.** הסשן השני הוסיף בדיקת קלט מוקדמת ורשימת היתר חדשה
  (`_shared/frontend.ts`). תוודא שהיא לא רצה **לפני** איסוף קבצי ה-carry-through,
  אחרת קובץ הגדרות ייפסל בדלת ולא יחזור למשתמש.
- **`fetch-repo`.** שנינו נגענו בלולאת הפריסה של ה-tar. תוודא שהנתיב עדיין עובר
  דרך `safeRelPath` אחרי הסרת קידומת התיקייה.
- **`rebuild`.** תוודא ש-`balanceCss` עדיין רצה לפני הצירוף, ולא הוזזה אחריו.

---

## איך לאמת בלי לשלם

הכלל: אל תריץ בנייה כדי לבדוק אם קוד רץ. תוכיח קודם שהוא רץ.

1. **בדיקות יחידה** — `deno task test`. אמור להיות 296 ומעלה, אפס נפילות.
2. **בדיקת טיפוסים** — `deno task check`. תופסת ייבוא יתום שנשאר אחרי המחיקות.
3. **חיפוש הפניות מתות** — `transform`, `design`, `apply`, `qa` לא אמורים להופיע
   בשום קריאה ב-`js/app.js`, ולא כמזהה ב-HTML של שני הדשבורדים.
4. **הדשבורד בדפדפן** — הרם שרת סטטי, טען את `/app/` ואת `/he/app/`, וודא אפס
   שגיאות קונסול ושהכפתורים שנמחקו לא קיימים.
5. **סינון וקבצים חוזרים** — אפשר לאמת את `keepPath`, `safeRelPath` ו-
   `isCarryThrough` בלי דפדפן בכלל: הן פונקציות טהורות. שולפים אותן מ-`js/app.js`
   ומריצים ב-node על רשימת נתיבים. ככה נמצא באג אמיתי בסשן הקודם — `keepPath` אישר
   `/etc/passwd` כי הוא רק בדק ולא ניקה.
6. **חבילות שמורות** — אם יש ריצה קודמת ב-Storage, אפשר להריץ עליה את הצינור
   מקומית ולראות מה קורה, בלי קריאה למודל ובלי לשלם.

**רק אחרי שכל זה ירוק** — ריצה אמיתית אחת על אתר קטן. מה למדוד בה:
- הציון לפני ואחרי, מול המקור ולא לבד.
- מה כתוב ב-`self_check` של הריצה: אילו אזהרות, מה `css_balance` מדווח.
- שההורדה מכילה את קבצי ההגדרות שהעלית, בלי שינוי.
- שפתיחת PR מתנהגת לפי הטבלה למעלה.

---

## כללי ברזל שאסור לשבור בזמן התיקון

- **אף פעם לא מסרבים למסור.** ממצא נוסע לצד התוצאה, לא במקומה. המשתמש שילם, הוא
  מקבל, והוא מחליט.
- **קוד עדיף על שאלה למודל** כשהשאלה היא ספירה או רשימה סגורה.
- **מודדים מול המקור**, לא מול מספר יפה לבד.
- **תיקון שלא אומת הוא לא תיקון.** אם אי אפשר להוכיח שהוא נחת, אל תדווח שהוא נחת.
